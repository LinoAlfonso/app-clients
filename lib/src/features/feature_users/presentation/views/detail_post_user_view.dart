import 'package:flutter/material.dart';

class DetailPostUserView extends StatelessWidget {
  static const routeName = '/detail-post-user';
  const DetailPostUserView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Detail Post User Screen'),
      ),
    );
  }
}